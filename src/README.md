# Source Code

This directory contains the core AlphaTrace platform source code.

## Structure

- `/components` - Reusable UI components
- `/services` - Core business logic and API services
- `/utils` - Utility functions and helpers
- `/types` - TypeScript type definitions

## Development

Run `npm run dev` from the root directory to start development mode.